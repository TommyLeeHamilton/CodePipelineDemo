apiVersion: v1
kind: Service
metadata:
  name: octank-invogue
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: octank-invogue